# Runtime used by Speekium's Python ASR sidecar. mlx-whisper itself is
# installed from requirements-macos-arm64-nodeps.txt so its conversion-only
# PyTorch dependency is not shipped in the transcription runtime.
# These versions are the known-good Apple Silicon environment used to build
# the app. Keep both requirements files in sync when upgrading the model stack.
mlx==0.29.4
mlx-qwen3-asr==0.3.5
numpy==2.5.1
huggingface-hub==1.26.0
numba==0.67.0
scipy==1.18.1
tiktoken==0.14.0
tqdm==4.70.0
